"""Webapp package: FastAPI app factory, API routes and the static SPA shell.

The dashboard is a no-build vanilla-JS single-page app driven by the API
routes registered in :mod:`frigate_learn.webapp.api`.
"""